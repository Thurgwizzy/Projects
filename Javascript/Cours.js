/*
	document.write() --afficher sur la page
	alert() / confirm() --affiche un pop-up / affiche un pop-up avec confirmation
	console.log() --afficher dans la console
	console.error() --afficher une erreur dans la console
	prompt() --équivalent de inputbox
	typeof --type de variable
	parseInt() --conversion de type integer
	parseFloat() --conversion de type double
	var --déclaration de variable utilisable dans tout le code ou dans une fonction
	let --déclaration de variable utilisable dans un bloc de code
	const --déclaration de variable constante	
*/

/*
	--Structure conditionnelle avec if
	
		if(condition à vérifier)
			instruction si vraie;
		else if(condition à vérifier)
			instruction si vraie;
		else (cas par defaut)
			instruction;
		
	--Structure conditionnelle avec switch(comme case en vba)
	
		switch(variable à vérifier)
		{
			case 0:
				instruction pour le 1er cas
				break; (pour sortir du code si la valeur est vraie)
				
			case 1:
				instruction pour le 2ème cas
				break; (pour sortir du code si la valeur est vraie)
				
			case n:
				instruction pour le nième cas
				break; (pour sortir du code si la valeur est vraie)
				
			default: (instruction par defaut)
			break; (pour sortir du code si la valeur est vraie)
		}
		
	--Opérations ternaires
		
		<condition> ? <cas_vrai> : <cas_faux>
*/